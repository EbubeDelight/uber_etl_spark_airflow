# For Linux

# Install Python and pip 
sudo apt-get update

sudo apt-get install python3-distutils

sudo apt-get install python3-apt

sudo apt-get install wget

wget https://bootstrap.pypa.io/get-pip.py

sudo python3 get-pip.py


# Install Mage
sudo pip3 install mage-ai

# Install Pandas

sudo pip3 install pandas

# Install Google Cloud Library
sudo pip3 install google-cloud

sudo pip3 install google-cloud-bigquery


# For Windows do the following
download and install python with this link: https://www.python.org/downloads/windows/

# pip usually comes installed with Python on Windows. You can verify by opening a command prompt and running
pip --version

# Install Airflow, Pandas, and Google Cloud Library
pip install apache-airflow
pip install pandas
pip install google-cloud
pip install google-cloud-bigquery

